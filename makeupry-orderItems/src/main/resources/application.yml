spring:
   application:
      name: ORDERITEM-SERVICE

server:
   port: 9002

eureka:
   client:
      register-with-eureka: true
      fetch-registry: true
      serviceUrl:
         defaultZone: http://localhost:8761/eureka/
   instance:
      hostname: "localhost"